services:
  - type: web
    name: urbanmood
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
      - key: MAILERSEND_API_KEY
        sync: false
